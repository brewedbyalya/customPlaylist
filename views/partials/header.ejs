<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Playlist app</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <header class="main-header">
        <div class="container header-container">
            <div class="logo">
                <a href="/">
                    <i class="fas fa-music"></i>
                    <span>PlaylistApp</span>
                </a>
            </div>
            
            <nav class="main-nav">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/playlists">Browse</a></li>
                    
                    <% if (user) { %>
                        <li><a href="/playlists/new">Create</a></li>
                        <li class="user-menu">
                            <span class="username">
                                <i class="fas fa-user-circle"></i> <%= user.username %>
                            </span>
                            <ul class="dropdown">
                                <li>
                                    <a href="#" class="btn-profile">
                                        <i class="fas fa-user"></i> Profile
                                    </a>
                                </li>
                                <li>
                                    <a href="/auth/sign-out" class="btn-logout">
                                        <i class="fas fa-sign-out-alt"></i> Sign Out
                                    </a>
                                </li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li><a href="/auth/sign-in" class="btn btn-secondary">Sign In</a></li>
                        <li><a href="/auth/sign-up" class="btn btn-primary">Sign Up</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>